# Scripts

## setup.sh

Script bash interactif pour:
- créer `.env`
- demander les variables (Neynar / x402)
- `npm install`, `npm run build`, `npm run dev`

Sur Windows: utilise WSL ou Git Bash.

## setup.ps1

Alternative PowerShell (Windows natif).

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\setup.ps1
```
